<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans:ital,wght@0,100..900;1,100..900&display=swap"
        rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.5.0/dist/chart.umd.min.js"></script>
    <script src="/js/chart.js"></script>
    <script src="/js/events.js"></script>
    <title>Compare Aircraft, Spacecraft Exports</title>
</head>

<body>
    <%- include('./partials/header') %>
        <main>
            <% if (error) { %>
            <%- include('./partials/error') %>
            <% } %>
            <section>
                <div class="container">
                    <div class="row">
                        <div class="col col-left">
                            <h1>Compare Aircraft & Spacecraft Exports</h1>
                            <p>Use the form on this page to compare two countries' aircraft and spacecraft exports.</p>
                            <% if (country1 && country2) { %>
                            <p>Currently comparing <strong><%= country1.name %></strong> and <strong><%= country2.name %></strong>.</p>
                            <% } %>
                        </div>
                        <div class="col col-right">
                            <%- include('./partials/form') %>
                        </div>
                    </div>
                </div>
            </section>
            <% if (country1 && country2) { %>
                <% if (country1.totalWorld && country2.totalWorld) { %>
                <section>
                    <div class="container">
                        <h2>World Overview</h2>
                        <p>This chart shows the total value of aircraft and spacecraft exports from <%= country1.name %> and <%= country2.name %> to the rest of the world.</p>
                        <%- include('./partials/chart', {id: "world-overview", title: "Total World Aircraft & Spacecraft Exports", labels: [country1.name, country2.name], values: [country1.totalWorld?.value, country2.totalWorld?.value]}) %>
                        <p>View this data for <a href="https://tradingeconomics.com<%= country1.totalWorld?.url%>" target="_blank"><%= country1.name %></a> and <a href="https://tradingeconomics.com<%= country2.totalWorld?.url%>" target="_blank"><%= country2.name %></a> on Trading Economics.</p>
                    </div>
                </section>
                <% } %>
                <% if (country1.toEachOther && country2.toEachOther) { %>
                <section>
                    <div class="container">
                        <h2>To Each Other</h2>
                        <p>This chart shows the value of aircraft and spacecraft exports from each country to the other (i.e., from <%= country1.name %> to <%= country2.name %> and from <%= country2.name %> to <%= country1.name %>).</p>
                        <%- include('./partials/chart', {id: "to-each-other", title: "Aircraft & Spacecraft Exports to Each Other", labels: [country1.name, country2.name], values: [country1.toEachOther?.value, country2.toEachOther?.value]}) %>
                        <p>View this data for <a href="https://tradingeconomics.com<%= country1.toEachOther?.url%>" target="_blank"><%= country1.name %></a> and <a href="https://tradingeconomics.com<%= country2.toEachOther?.url%>" target="_blank"><%= country2.name %></a> on Trading Economics.</p>
                    </div>
                </section>
                <% } %>
                <% if (country1.topCountries && country2.topCountries) { %>
                <section>
                    <div class="container">
                        <h2>Top Countries</h2>
                        <p>Below are the top 20 countries receiving the most aircraft and spacecraft exports from <%= country1.name %> and <%= country2.name %>.</p>
                        <div class="row">
                            <div class="col col-left">
                                <%- include('./partials/top-countries', {name: country1.name, topCountries: country1.topCountries }) %>
                            </div>
                            <div class="col col-right">
                                <%- include('./partials/top-countries', {name: country2.name, topCountries: country2.topCountries }) %>
                            </div>
                        </div>
                    </div>
                </section>
                <% } %>
            <% } %>
        </main>
</body>
</html>